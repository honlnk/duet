{
  "name": "duet",
  "version": "1.0.0",
  "private": true,
  "description": "Duet — 双 AI 自主对话聊天室",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @duet/web build && node server/src/index.js",
    "build": "pnpm --filter @duet/web build",
    "start": "cross-env NODE_ENV=production node server/src/index.js",
    "test": "node scripts/test-e2e.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "ws": "^8.18.0"
  }
}
